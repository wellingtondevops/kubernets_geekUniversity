#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../yaml-language-server/bin/yaml-language-server" "$@"
  ret=$?
else 
  node  "$basedir/../yaml-language-server/bin/yaml-language-server" "$@"
  ret=$?
fi
exit $ret
